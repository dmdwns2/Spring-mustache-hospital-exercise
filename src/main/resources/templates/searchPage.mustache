{{>layout/header}}

<table class="table">
    <thead class="thead-light">
    <tr>
        <th scope="col">ID</th>
        <th scope="col">제목</th>
        <th scope="col">내용</th>

    </tr>
    </thead>
    {{#searchList}}
        <tbody>
        <tr>
            <td><a href="/{{id}}">{{title}}</a></td>
            <td>{{title}}</td>
            <td>{{content}}</td>
        </tr>
        </tbody>
    {{/searchList}}
</table>

{{>searchForm}}

{{>layout/footer}}